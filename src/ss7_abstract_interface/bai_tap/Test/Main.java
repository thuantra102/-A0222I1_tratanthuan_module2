package ss7_abstract_interface.bai_tap.Test;

public class Main {
    public static void main(String[] args) {
        Car car = new Car();
        car.go();
        // An abstract class can no be impliment like an nomal object but their subclass can be
        // The differences between an abstract and interface are
        // An interface contains all the abstract method
        // AN interface can not be extents so instead of the we use impliment and appear the mutiple chillclass in java

    }
}
